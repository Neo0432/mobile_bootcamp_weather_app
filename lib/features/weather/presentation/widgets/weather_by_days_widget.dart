import 'package:flutter/material.dart';

class WeatherByDaysWidget extends StatelessWidget {
  const WeatherByDaysWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
